import 'package:flutter/material.dart';

class SafeBlockPage extends StatelessWidget {
  const SafeBlockPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('SafeBlock Engine Module'),
      ),
    );
  }
}
