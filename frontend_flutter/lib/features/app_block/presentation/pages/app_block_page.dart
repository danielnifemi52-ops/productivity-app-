import 'package:flutter/material.dart';

class AppBlockPage extends StatelessWidget {
  const AppBlockPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('AppBlock Engine Module'),
      ),
    );
  }
}
